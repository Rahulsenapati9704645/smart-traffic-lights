def load_vehicle_locations(image_path):
    """
    Dummy function to simulate loading vehicle locations.
    Args:
        image_path (str): Path to the image file.
    Returns:
        list of tuples: List of vehicle center coordinates [(x1, y1), (x2, y2), ...].
    """
    # This should be replaced with actual logic to load vehicle data.
    return [(100, 150), (200, 250), (300, 350)]
